<% include partials/header %>

<div class="container">
    <div class="row text-center" style="display:flex; flex-wrap: wrap;" id = "product">
        <% products.forEach(function(product){ %>
            <div class="col-lg-4 col-sm-6">
                <div class="thumbnail">
                    
                    <img src="<%= product.image %>" style="height:300px; width: 250px">
                    <div class="caption">
                        <h4><%= product.title %></h4>
                    </div>
                    <p>
                        <%= product.description %>
                    </p>
                    <%= product.price %>$
                    <div>
                        <button onclick="onClick()" class="btn btn-success">Add to Cart</button>
                    </div>
                    
                </div>
            </div>
              
          
        <% }); %>
    </div>   
</div>


<% include partials/footer %>
